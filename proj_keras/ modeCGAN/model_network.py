import tensorflow as tf

from tensorflow.keras.models import Sequential
from tensorflow.keras.layers import Dense, Dropout, Flatten, LeakyReLU, Reshape, Input, Embedding, Concatenate, Conv2D
from tensorflow.keras.optimizers import Adam
from tensorflow.keras.models import load_model
from tensorflow import keras
from tensorflow.keras import Model

def build_generator(latent_dim, num_classes = 10):
    

    

    


    return

def build_discriminator(img_shape = (28,28,1), num_classes = 10):
    
    return